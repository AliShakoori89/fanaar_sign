import 'package:flutter/material.dart';

class IssuedNewCertificate extends StatelessWidget {
  const IssuedNewCertificate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
